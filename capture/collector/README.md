# Pydump Collector

The Python Collector coordinates GDB attachment, owns heap-sized traversal state, and writes the
completed heap artifact. The injected C Agent is built separately from `capture/agent`.

The repository root README contains the supported capture command and development workflow.
